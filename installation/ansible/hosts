# Hadoop group of nodes
[hadoop:children]
master
slaves

# Master server
[master]
localhost

# List of slave servers
[slaves]
localhost

# Elasticsearch nodes
[elastic]
localhost

# Supervisor nodes
[supervisor]
localhost
